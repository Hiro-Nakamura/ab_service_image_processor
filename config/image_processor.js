module.exports = {
	/**
	 * The absolute path to the directory within the linux container where
	 * the input images will be read from.
	 */
	inputPath: '/tmp',
	
	/**
	 * The absolute path to the directory within the linux container where
	 * the output images will be written to.
	 */
	outputPath: '/data/image_processor',
	
};